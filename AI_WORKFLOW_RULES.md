# AI_WORKFLOW_RULES.md — 协作规则（Claude / Codex / ChatGPT）

> 项目：智学探险家 / AI 游戏化学生辅助教学
> 本文件固化后续多方协作的固定规则。所有执行方（Claude Code、Codex）在动手前应先读本文件。
> 本文件只描述规则，不含业务代码。

---

## 1. 角色与职责

| 角色 | 身份 | 职责 |
|---|---|---|
| **用户** | 产品负责人 | 拍板需求、优先级、验收；唯一可决定范围与发布的人。 |
| **ChatGPT** | 总设计师 / 技术架构师 | 出方案、定架构、写规格与任务拆解；产出给执行方的指令。 |
| **Claude（Claude Code）** | 分析师 / 临时执行 | 审计、排查、写文档；Codex 额度不足时临时顶上做执行开发。 |
| **Codex** | 执行开发 | 主要执行开发；按 ChatGPT 的规格落地代码。 |

**默认主执行方是 Codex**；Claude 在 Codex 不可用时临时执行，规则一致。

---

## 2. 视觉资产规则（重要）

- **正式视觉资产一律用 GPT Image 生成**（背景、立绘、图标、纪念卡等）。
- **禁止用 emoji / CSS 画的图标充当正式视觉资产**。
  - 仅允许 emoji/CSS 作为**临时占位或纯装饰性点缀**（如庆祝粒子、加载小点），不得作为关卡背景、角色、按钮主图等正式资产。
- 图片里**不得烘焙任何文字 / 数字 / 按钮**——所有文案由代码叠加（见各 `public/assets/*/README.md` 的负向词规则）。
- 资产文件名与 `lib/*Assets.ts` 一一对应，**不得随意重命名或移动**。

---

## 3. 分支规则

- **默认工作分支：`s2-adventure-map`**。
- 不在 `main` 上直接改；如不在该分支，先 `git checkout s2-adventure-map`。
- 每个小任务尽量保持提交聚焦、信息清晰，便于回溯与多方接力。

---

## 4. 禁止提交的内容

以下**绝不允许**进入任何提交：

- `.env.local`（真实环境变量 / 密钥）
- `.claude/`（本地工具配置）
- **任何真实 API key**（如 SiliconFlow / OpenAI key）——真 key 只放 `.env.local`（已被 `.gitignore` 忽略）；示例文件 `.env.local.example` 只保留占位符。
- 构建产物与临时文件：`next-env.d.ts` 的自动改动、`.next/`、各类 `*.log`、`tmp-*` 等。
- 提交前若发现 `next-env.d.ts` 被构建改动，先 `git checkout -- next-env.d.ts` 再提交。

---

## 5. 执行前后的固定动作

**每次执行前：**
- 先 `git status`（确认分支、工作区是否干净、有无意外改动）。

**每次执行后（改动代码时）：**
- `npm run typecheck`
- `npm run check`（eslint + tsc）
- `npm run build`
- **三条必须全部通过**才算完成。纯文档改动可省略 build（按任务说明）。

**提交前：**
- `git status --short` 确认只包含本次预期文件，未夹带禁止项（见第 4 节）。

---

## 6. 沟通与指令规则

- **小任务用短指令**，不重复粘贴长背景。
  - 例：「RC7.7 修 X 文案」即可，无需每次重述项目全貌。
- 背景、架构、长规格统一沉淀在文档里（本文件、`PROJECT_STATUS.md`、`PROJECT_HANDOVER.md`、`RISK_REPORT.md`、各 README），需要时引用，不反复粘贴。
- 任务命名沿用 `S阶段 / RCx.y 简述` 的格式，便于追踪。
- 执行方完成后按既定格式简洁汇报：分支、commit、验证结果、检查结论。

---

## 7. 范围纪律

- **只做被要求的事**；超出范围的改动先确认，不自行扩大。
- 不重构既有架构（如 `AdventureRunner` 状态机）除非明确要求；优先用**最小配置字段 + 最小渲染分支**。
- 关卡内容改 `lib/adventures.ts`，**不在引擎组件里写死单关内容**。
- 发现范围外的问题，记录/上报，不顺手乱改。
